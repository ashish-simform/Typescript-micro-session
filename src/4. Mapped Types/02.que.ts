export interface Example {
  name: string;
  age: number;
  id: string;
  organizationId: string;
  groupId: string;
}

/**
type Result = {
    id: string;
    organizationId: string;
    groupId: string;
}
 */
